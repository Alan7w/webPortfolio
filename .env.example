# Required when deploying. Use a long, unique password (12+ characters).
ADMIN_PASSWORD=
# Public origin, required for production (e.g. https://your-domain.com).
APP_ORIGIN=http://localhost:5173
PORT=5173
# Persistent directory; mount a persistent volume when deploying.
DATA_DIR=./data
